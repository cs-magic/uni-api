# 目标表格的配置
TARGET_TABLE_CONFIG = {
    "name": "Summary of project findings and ratings",
    # 可以在这里添加其他配置项，比如:
    # "alternative_names": [...],  # 其他可能的表格名称
    # "context_keywords": [...],   # 上下文关键词等
} 